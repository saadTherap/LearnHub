rootProject.name = 'LearnHub'
include('authserver', 'authcommon', 'authkeyprovider', 'courseConfigurator', 'secureFileServer', 'learningProcessor',
        'service-registry', 'hazelcast-server')

include 'authkeyprovider'