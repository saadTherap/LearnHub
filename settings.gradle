rootProject.name = 'LearnHub'
include('authserver', 'auth-common', 'courseConfigurator', 'secureFileServer', 'learningProcessor', 'service-registry', 'hazelcast-server', 'kafka-registry', 'infra:cache-support')


